# Docker Compose injects these DB values for containers.
# Keep them here for local runs or manual overrides.
SERVER_PORT=8080
DATASOURCE_URL=jdbc:postgresql://localhost:5432/studydocs_education_db
DATASOURCE_USERNAME=postgres
DATASOURCE_PASSWORD=postgres
